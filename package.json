{
  "name": "radio",
  "version": "1.0.0",
  "description": "Allows streaming of music from hard drive and remote control features",
  "scripts": {
    "build": "gulp build",
    "deploy": "npm run build && npm run start",
    "lint": "tslint src/**/*.ts || exit 0",
    "start": "node build/server/server.node.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "file://z:\\\\Radio"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-typescript": "^2.12.0",
    "tslint": "^3.5.0",
    "typescript": "^1.8.2",
    "typings": "^0.6.8"
  }
}
