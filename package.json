{
  "name": "seed",
  "version": "1.0.0",
  "description": "Project seed for server with front end in typescript",
  "scripts": {
    "build": "gulp build",
    "deploy": "npm run build && npm run start",
    "lint": "tslint src/**/*.ts || exit 0",
    "start": "node build/server/run.node.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cleavera/ProjectSeed.git"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-typescript": "^2.12.0",
    "tslint": "^3.5.0",
    "typescript": "^1.8.2",
    "typings": "^0.6.8"
  },
  "dependencies": {
    "node-uuid": "^1.4.7"
  }
}
